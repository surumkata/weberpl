/* CLASSE AUXIALIRES */
export class Position {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  set(x,y){
    this.x = x;
    this.y = y;
  }

}

export class Size {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }

  set(x,y){
    this.x = x;
    this.y = y;
  }

}
